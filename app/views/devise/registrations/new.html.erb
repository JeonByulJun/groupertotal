<style>
body
{
background-image:url('/bgpic.jpg');
background-repeat:no-repeat;
background-size: cover;
}
</style>
<div style="text-align:center">
  <div class="row">
    <br><br><br><br><br><br><br><br>
    <div class="col-sm-4 col-sm-offset-4">
      <div class="panel panel-default">
        <div class="panel-body">

          <h2><%=@email%></h2>
          <p>비밀번호만 설정하면 가입이 완료됩니다.</p>
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= devise_error_messages! %>
          
            <div class="field">
              <%= f.email_field :email, autofocus: true, value: @email, hidden: true %>
            </div>
            
            <div class="field">
              <%= f.text_field :last_name, required: true, value: "성 설정하기", hidden: true %>
            </div>
            
            <div class="field">
              <%= f.text_field :first_name, required: true, value: "이름 설정하기", hidden: true %>
            </div>
          
            <div class="field">
              <% if @minimum_password_length %>
                <%= f.password_field :password, class: "form-control", autocomplete: "off", required: true, placeholder: "비밀번호(최소 "+@minimum_password_length.to_s+"자)" %>
              <% else %>
                <%= f.password_field :password, class: "form-control", autocomplete: "off", required: true, placeholder: "비밀번호" %>
              <% end %>
            </div>
          
            <div class="field">
              <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "off", required: true, placeholder: "확인" %>
            </div>
          
            <div class="actions">
              <button type="submit" class="btn btn-link">가입 완료</button>
            </div>
          <% end %>
          
        </div>
      </div>
    </div>
  </div>
</div>
