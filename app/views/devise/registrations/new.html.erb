<style>
body
{
background-image:url('/bgpic.jpg');
background-repeat:no-repeat;
background-size: cover;
}
</style>
<div style="text-align:center">
  <div class="row">
    <br><br><br><br><br><br><br><br>
    <div class="col-sm-4 col-sm-offset-4">
      <div class="panel panel-default">
        <div class="panel-body">

          <h2 style="margin-top: 0px;word-wrap:break-word; word-break:break-all;"><%=@email%></h2>
          <p>이름과 비밀번호만 설정하면 가입이 완료됩니다.</p>
          <div class="row">
            <div class="col-sm-8 col-sm-offset-2">
              <% if !flash[:notice].blank? %>
                <div class="well well-sm">
                  <%= flash[:notice] %>
                </div>
              <% end %>
            </div>
          </div>
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= devise_error_messages! %>

            <div class="field">
              <%= f.email_field :email, autofocus: true, value: @email, hidden: true %>
              <%= f.text_field :auth, value: @auth, hidden: true %>
            </div>

            <div class="field">
              <%= f.text_field :last_name, required: true, value: "왜만듬?", hidden: true %>
            </div>
            <div class="row">
              <div class="col-sm-8 col-sm-offset-2">
                <%= f.text_field :first_name, class: "form-control", required: true, placeholder: "이름" %>
                <% if @minimum_password_length %>
                  <%= f.password_field :password, class: "form-control", autocomplete: "off", required: true, placeholder: "비밀번호(최소 "+@minimum_password_length.to_s+"자)" %>
                <% else %>
                  <%= f.password_field :password, class: "form-control", autocomplete: "off", required: true, placeholder: "비밀번호" %>
                <% end %>
                  <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "off", required: true, placeholder: "비밀번호 확인" %>
                <br>
                <button type="submit" class="btn btn-new" style="width: 100%;">가입 완료</button>
              </div>
            </div>

            <div class="actions">
            </div>
          <% end %>

        </div>
      </div>
    </div>
  </div>
</div>
